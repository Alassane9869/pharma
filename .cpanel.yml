---
deployment:
  tasks:
    - export DEPLOYPATH=/home/vuxe8870/Souley-Guirou.danayaplus.com
    - /usr/bin/rsync -a --exclude='.git' . $DEPLOYPATH/
    - /bin/chmod -R 755 $DEPLOYPATH 2>/dev/null || true
    - /usr/bin/find $DEPLOYPATH -type f -exec /bin/chmod 644 {} + 2>/dev/null || true
